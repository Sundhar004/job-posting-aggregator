services:
  web:
    build:
      context: ../
      dockerfile: docker/Dockerfile
    container_name: job-tracker-app
    ports:
      - '3000:3000'
    env_file:
      - .env
    environment:
      - PORT=3000
      - NODE_ENV=production
    restart: always
